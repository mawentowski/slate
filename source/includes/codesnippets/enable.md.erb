```shell
curl --location -g --request POST 'https://sita-{{Env}}-{{AzRegion}}-apimanagement.azure-api.net/api/v2/peripheral/enable' \
--header 'Authorization: {{bearer_token}}' \
--header 'content_type: application/json' \
--data-raw '{ 
 "device_ID" : "{{device_ID}}", 
 "transaction_ID" : "62d1a8e4-9e0c-4959-97ff-59342fb2c33f", 
 "session_token" : "{{session_token}}"
 }
'
```

```javascript
var myHeaders = new Headers();
myHeaders.append("Authorization", "{{bearer_token}}");
myHeaders.append("content_type", "application/json");

var raw = JSON.stringify({"device_ID":"{{device_ID}}","transaction_ID":"26cf158a-f8cf-43d7-a16a-b8b749aaa57d","session_token":"{{session_token}}"});

var requestOptions = {
  method: 'POST',
  headers: myHeaders,
  body: raw,
  redirect: 'follow'
};

fetch("https://sita-{{Env}}-{{AzRegion}}-apimanagement.azure-api.net/api/v2/peripheral/enable", requestOptions)
  .then(response => response.text())
  .then(result => console.log(result))
  .catch(error => console.log('error', error));
```

```java
OkHttpClient client = new OkHttpClient().newBuilder()
  .build();
MediaType mediaType = MediaType.parse("application/json");
RequestBody body = RequestBody.create(mediaType, "{ \r\n \"device_ID\" : \"{{device_ID}}\", \r\n \"transaction_ID\" : \"9b673d5c-3790-43ea-9308-6d57f20bdd00\", \r\n \"session_token\" : \"{{session_token}}\"\r\n }\r\n");
Request request = new Request.Builder()
  .url("https://sita-{{Env}}-{{AzRegion}}-apimanagement.azure-api.net/api/v2/peripheral/enable")
  .method("POST", body)
  .addHeader("Authorization", "{{bearer_token}}")
  .addHeader("content_type", "application/json")
  .build();
Response response = client.newCall(request).execute();
```

```swift
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

var semaphore = DispatchSemaphore (value: 0)

let parameters = "{ \r\n \"device_ID\" : \"{{device_ID}}\", \r\n \"transaction_ID\" : \"c6874c0b-df3d-4ac7-b9aa-a50500e91061\", \r\n \"session_token\" : \"{{session_token}}\"\r\n }\r\n"
let postData = parameters.data(using: .utf8)

var request = URLRequest(url: URL(string: "https://sita-{{Env}}-{{AzRegion}}-apimanagement.azure-api.net/api/v2/peripheral/enable")!,timeoutInterval: Double.infinity)
request.addValue("{{bearer_token}}", forHTTPHeaderField: "Authorization")
request.addValue("application/json", forHTTPHeaderField: "content_type")

request.httpMethod = "POST"
request.httpBody = postData

let task = URLSession.shared.dataTask(with: request) { data, response, error in 
  guard let data = data else {
    print(String(describing: error))
    semaphore.signal()
    return
  }
  print(String(data: data, encoding: .utf8)!)
  semaphore.signal()
}

task.resume()
semaphore.wait()

```


> Request Body (JSON):

```json
{ 
    "device_ID" : "{{device_ID}}", 
    "transaction_ID" : "{{$guid}}", 
    "session_token" : "{{session_token}}"
}

```