## Make API Calls

<%= partial "includes/codesnippets/auth.md.erb" %>

Prior to making your first API call, add the bearer token retrieved by a Request Bearer Token request in the header, as shown below.
Include it in all your requests for authorization.

<!-- Our web-hosted resources are protected through 
the Microsoft identity platform that implements OAuth 2.0 authorization protocol. 
OAuth 2.0 is a method through which your third-party app can access web-hosted 
resources on behalf of your users. -->

<%= partial "includes/sections/headerparams.md.erb" %>

<aside class="notice">
You must replace <code>{{BearerToken}}</code> in the header of all your requests with the access token returned after sending the GetBearerToken request.
</aside>

Continue onward to make your first Subscribe call.

